---
"@rgrmdesign/rgrm-ds-tokens": minor
---

Move `radius` and `size` tokens from core to the scale collection. Button `border-radius` and `min-block-size` now reference fluid scale tokens (`--rgrm-scale-radius-*`, `--rgrm-scale-size-*`).
